'use client';

/**
 * <RankingList type={...} limit={...} />
 *
 * 범용 랭킹 리스트.
 * type prop으로 데이터 소스를 분기 (useRanking 호출).
 *
 * 표시:
 *   - 순위 뱃지 (1~5)
 *   - 여행지 썸네일
 *   - 이름 + 카테고리 + 짧은 설명
 *   - (옵션) 우승 횟수 / 점수
 *
 * 카드 클릭 → 여행지 상세 (추후) 또는 토너먼트 시작 옵션 모달
 */
export function RankingList() {
  return null;
}
