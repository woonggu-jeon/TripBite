# 화면 드리프트 — Figma `페이지` ↔ 레포 라우트

> 4단계(화면 구성) 작업의 근거 문서. Figma 화면 62개와 레포 라우트 30개를 대조한다.
> 작성 시작: 2026-08-03. **진행 중 — 아래 "검토 상태"에 표시된 화면만 실제 비교됨.**

---

## 0. 읽는 방법

**드리프트 등급**

| 등급  | 뜻                                                   | 작업 성격                         |
| ----- | ---------------------------------------------------- | --------------------------------- |
| **A** | 구조가 다름 — 섹션 종류·순서·구성 요소가 어긋남      | UI 재작업 (기능·데이터 영향 가능) |
| **B** | 구조는 같고 스타일이 다름 — 탭 모양, 배너 형태, 여백 | 스타일 수정                       |
| **C** | 거의 일치 — 미세 여백/색만                           | 소소한 정리                       |
| **?** | 미검토                                               | —                                 |

### ⚠ 드리프트로 세지 않는 것 (환경 한계)

비교는 목 모드(`NEXT_PUBLIC_USE_MSW=true`, worktree `../TripBite-verify`, 포트 3902)에서
했다. 다음은 **실 API 에서는 정상이므로 드리프트가 아니다.**

1. **이미지 자리에 이모지가 보임** — `MediaThumb` 이 `src` + `emoji` fallback 구조이고
   컴포넌트들이 이미 `imageUrl` / `heroImage` / `photos` 를 소비한다. 목 시드가
   `imageUrl: null` 이고 생성 faker 목은 URL 이 아닌 임의 문자열을 주기 때문에
   항상 fallback 이 나온다. 실 API 이미지가 붙으면 사진이 표시된다.
2. **목록이 빈 상태로 나옴** — 목 데이터 부족.
3. **PWA 업데이트 배너 / dev 오버레이 / 우하단 배지** — 개발 환경 요소. 유지 결정됨.

> ⚠ **PWA 배너는 sticky 헤더를 가린다** (높이 60px, `top: 0`). 이 때문에 초판에서
> "헤더 부재" 를 오판했다 (§3 정정). 캡처 시 배너를 DOM 에서 제거할 것:
>
> ```js
> [...document.querySelectorAll('div,section,aside')]
>   .filter(
>     (e) =>
>       /새 버전이 있어요|앱처럼 빠르게/.test(e.textContent || '') &&
>       e.getBoundingClientRect().top < 80 &&
>       e.getBoundingClientRect().height < 140,
>   )
>   .forEach((e) => e.remove());
> ```

### 문구는 이 문서의 대상이 아니다

Figma 문구와 앱 문구가 달라도 **드리프트로 적지 않는다.** 문구는 나중에 일괄 정리하기로
했고, SoT 는 `src/i18n/messages/*.json` 이다 ([HANDOFF.md §0-1](./HANDOFF.md)).
단 "탭이 3개인데 이름이 다름" 처럼 **구성 요소 수·의미가 달라지는 경우는 적는다.**

---

## 1. 화면 매핑 · 드리프트 요약

### 검토 완료

| Figma 화면       | 라우트        | 등급       | 요지                                           |
| ---------------- | ------------- | ---------- | ---------------------------------------------- |
| `HOME · 홈`      | `/`           | **A→반영** | hero·카드·빠른시작 반영. 칩 필터만 보류 (§3-2) |
| `편지 메인`      | `/letter`     | **A→반영** | 배너·탭·목록 항목 반영 (§3-2)                  |
| `TRN · 토너먼트` | `/tournament` | **C→반영** | 제목 재배치 + `big-card`. 확정 플로우는 유지   |
| `설정` ×3        | `/settings`   | **B→반영** | 풀블리드 행 + 하단 `bw` (§3-2)                 |

### 검토 완료 (2차 — 9개)

| Figma 화면              | 라우트                      | 등급       | 요지                                        |
| ----------------------- | --------------------------- | ---------- | ------------------------------------------- |
| `TST · 유형테스트 질문` | `/quiz`                     | **C→반영** | 카운터 제거 + 세그먼트/선택지 규격          |
| `편지 쓰기`             | `/letter/compose`           | **C→반영** | 연결형 5칸 + 회색 카드 + 단일 버튼          |
| `RNK · 랭킹`            | `/ranking`                  | **B→반영** | `rv-card` 묶음 + 1위 hero + 구분선 행       |
| `알림 목록`             | `/notifications`            | **B→반영** | 풀블리드 행 + 원형 아이콘 + 상대시각        |
| `RGN · 시군상세`        | `/region/[code]`            | **B→반영** | 단색 배너 + pill 칩 + 카드                  |
| `MY_01 · 마이페이지`    | `/mypage`                   | **A→반영** | 헤더 톱니 + 프로필 블록 + 진행률 바         |
| `POI · 장소상세`        | `/destination/[id]`         | **C→반영** | 풀블리드 hero + 정보 순서 + 2버튼 (§2.11-1) |
| `MY · 충북 도장책`      | `/mypage/stamps`            | **C→반영** | 진행률 카드 + 지도 카드 + 범례 (§2.11-2)    |
| `MY · 저장한 우승지`    | `/mypage/saved-tournaments` | **B→반영** | 여백·하트·빈상태·모달 규격 (§2.11-3)        |

### 미검토 (캡처 미확보)

auth 8개 (`SPLASH` `LOGIN · A` `AUTH · 회원가입` ±error `회원가입 완료`
`아이디 찾기` ±결과 `비밀번호 찾기`) · `Walk 1~4` · 토너먼트 T-3~T-8 ·
편지 하위 6개 · 빈 상태 변형 다수 · 공유 카드 3개

> Figma 62개 중 **상당수가 같은 화면의 상태 변형**(빈 상태 / 클릭시 / error / 삭제)이다.
> 고유 화면은 약 30개로, 레포 라우트 수와 비슷하다.

---

## 2. 화면별 상세

### 2.1 `HOME · 홈` → `/` — 등급 A

| 항목        | Figma                                                        | 현재                                                 |
| ----------- | ------------------------------------------------------------ | ---------------------------------------------------- |
| 히어로      | 사진 배경 카드 1장, "오늘의 여행" eyebrow + 20px 제목 + 위치 | **처리 완료** — 사진 배경 히어로 (캐러셀 유지, §3-1) |
| 섹션 2 제목 | "이런 여행 어때요?" + `더보기 >`                             | "곧 충북에서 열리는 축제"                            |
| 섹션 2 필터 | **칩 4개** (전체/관광지/축제/체험관광)                       | **없음**                                             |
| 카드        | 정사각 사진 + 제목 + 위치(핀 아이콘)                         | 카드 + D-day 배지 + 카테고리 + 제목                  |
| 진입 배너   | 초록/주황 2개, 각 원형 배지 + 2줄 + **오른쪽 "시작" 버튼**   | 유사 구조, 버튼 없음                                 |
| 헤더        | 종 · 로고 · 설정                                             | 종 · (dev 배지들) · 로고 · 설정                      |

**작업 성격** — 칩 필터는 상태·필터링 로직이 필요(기능 추가). 히어로를 캐러셀에서
단일 카드로 바꾸는 것은 컴포넌트 교체. 배너 버튼 추가는 스타일+동작.

### 2.2 `편지 메인` → `/letter` — 등급 A

| 항목      | Figma                                                        | 현재                                                |
| --------- | ------------------------------------------------------------ | --------------------------------------------------- |
| 헤더      | 뒤로가기 + 중앙 타이틀                                       | **일치** ✓ (FAB 제거 후 확인)                       |
| 상단 배너 | 초록 1px 테두리 + 연초록 면 + 원형 배지 + 텍스트 링크        | 그라디언트 이미지 카드 + 초록 원형 화살표 버튼      |
| 탭        | **밑줄 탭** 3개                                              | **알약 세그먼트** 3개                               |
| 탭 항목   | 받은 / 보낸 / 저장한                                         | 받은 / 보낸 / 하트 ← 의미가 다를 수 있음, 확인 필요 |
| 목록 카드 | 원형 아바타 + 제목 + 시군 칩 + 작성자 + 시간 + 북마크 아이콘 | 목 데이터 없어 미확인                               |
| 하단 nav  | 5탭 평면                                                     | **일치** ✓                                          |

### 2.3 `TRN · 토너먼트` → `/tournament` — 등급 C

카드 2개(계절 테마 / 랜덤 테마) 선택 구조가 **일치**한다. 아이콘 + 제목 + 설명 구성도 같다.

| 항목      | Figma                                                   | 현재                                 |
| --------- | ------------------------------------------------------- | ------------------------------------ |
| 상단      | 뒤로가기 + "토너먼트" 헤더 + 24px 질문 제목 + 14px 설명 | 오판 — 헤더 존재 (§3 정정), 설명문만 |
| 선택 카드 | 선택 시 초록 테두리 + 연초록 면                         | 선택 상태 미확인                     |
| 하단 CTA  | 고정 "다음" 버튼                                        | 없음 (선택 즉시 진행)                |

### 2.5 `RNK · 랭킹` → `/ranking` — 등급 B

TOP5 목록 + "시군별 우승 횟수" 막대 11개 구조가 **일치**한다.

| 항목          | Figma                                  | 현재                                    |
| ------------- | -------------------------------------- | --------------------------------------- |
| 헤더          | 뒤로가기 + "여행지 랭킹"               | 오판 — 헤더 존재 (§3 정정)              |
| TOP5 1위      | 큰 사진 카드 + 이름/시군 오버레이      | 다른 항목과 같은 행 (금색 원형 순위)    |
| TOP5 2~5위    | 원형 썸네일 + 이름 + "시군 · N회 우승" | 원형 순위 배지 + 이름 + 시군 + 우측 N회 |
| TOP5 컨테이너 | 카드 하나 안에 5개 묶음                | 항목마다 개별 카드                      |
| 막대 목록     | 카드 하나 안에 11행, 행 구분선         | 행마다 개별 카드                        |

→ **1위 강조(사진 카드)** 와 **묶음 카드 vs 개별 카드** 가 핵심 차이.

### 2.6 `RGN · 시군상세` → `/region/[code]` — 등급 B

히어로 + 칩 필터 4개(전체/관광지/축제/체험관광) + 카드 그리드 구조가 **일치**한다.
홈에는 없던 칩 필터가 이 화면에는 이미 있다.

| 항목        | Figma                                                  | 현재 (개선 후)                   |
| ----------- | ------------------------------------------------------ | -------------------------------- |
| 히어로      | 단색 #EAF6EF 면, 320x103, 썸네일 없음 (사진 배경 아님) | 동일 — `--color-primary-soft` 면 |
| 카드 그리드 | 2열, 사진 + 제목 + 위치                                | 2열, 이미지 + 시군 + 제목 + 설명 |

> **정정** — 이 표의 이전 판은 히어로를 "사진 배경 + 시군명 오버레이" 로 적었으나
> **오판이었다.** Plugin API 로 `3413:7074` 를 열어보니 화면 전체에 IMAGE fill 이
> 하나도 없고, `banner` 프레임은 단색 `#EAF6EF` + 1px `#E0E0E0` 보더이며 텍스트도
> 흰색이 아니라 `#151515`/`#393939` 다. 사진 배경 히어로는 **홈과 랭킹 1위뿐**이다.
> 스크린샷만 보고 세 화면을 묶어 판단한 것이 원인 — 코드/노드 데이터로 확인해야 한다.

### 2.7 `TST · 유형테스트 질문` → `/quiz` — 등급 C

진행바 5칸 + 질문(24px) + 선택지 카드 4개 + "프로그레스를 누르면…" 안내문까지 **일치**.
차이는 `1/5` 카운터 표시뿐 (헤더는 양쪽 다 있음).

### 2.8 `편지 쓰기` → `/letter/compose` — 등급 C

5칸 글자 입력 + `N/5` 카운터 + 익명 체크박스 + 위치 행 + 하단 "편지 보내기" 구조 **일치**.

| 항목    | Figma                         | 현재                          |
| ------- | ----------------------------- | ----------------------------- |
| 입력 칸 | 밑줄 연결형 5칸 (한 덩어리)   | 개별 라운드 박스 5개          |
| 위치 행 | 핀 + 2줄 + "자동 설정됨" 배지 | 핀 + 1줄 (권한 거부 상태)     |
| 하단    | "편지 보내기" 1개             | "또 쓰기" + "편지 보내기" 2개 |

### 2.9 `MY_01 · 마이페이지` → `/mypage` — 등급 A

섹션 순서(프로필 → 도장책 → 저장한 우승지 → 최근 토너먼트)는 같으나 **각 섹션 구성이 다르다.**

| 섹션          | Figma                                             | 현재                                      |
| ------------- | ------------------------------------------------- | ----------------------------------------- |
| 프로필        | 좌측 아바타(카메라 배지) + 닉네임 + 등급 칩, 가로 | 큰 카드 안 중앙 정렬 아바타 + 닉네임      |
| 도장책        | 카드 안 **진행률 바 + 8/11** + 안내 2줄           | 링크 행 1개 ("지도 보러 가기")            |
| 저장한 우승지 | 가로 스크롤 사진 카드 + `전체 보기 >`             | 빈 상태 (목 데이터 없음)                  |
| 최근 토너먼트 | 카드 안 트로피 배지 + 제목 + 메타 2행             | 빈 상태                                   |
| 하단          | —                                                 | "설정으로 이동" 버튼 (Figma 는 헤더 톱니) |

### 2.10 `알림 목록` → `/notifications` — 등급 B

행 구조(원형 배지 + 제목 + 설명 + 우측 시간 + 미읽음 점)는 컴포넌트에 이미 있다.

| 항목 | Figma                  | 현재                                    |
| ---- | ---------------------- | --------------------------------------- |
| 헤더 | 뒤로가기 + "알림"      | 오판 — 헤더 존재 (§3 정정)              |
| 상단 | 없음                   | **푸시 권한 요청 배너** (Figma 에 없음) |
| 목록 | 3행 (읽음/미읽음 구분) | 목 데이터 없어 빈 상태                  |

### 2.12 편지 5화면 재작업 (2026-08-05)

`편지 메인` 은 초판에서 A→반영이었지만 목록 행·상세·발송완료는 시안과 크게
달랐다. 다시 대조해 전부 맞췄다.

| 화면         | 시안                                                                                                | 이전 구현                                             |
| ------------ | --------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| 탭           | 받은 / 보낸 / **저장한**                                                                            | 받은 / 보낸 / **하트**                                |
| `letterItem` | stamp 48 r8 + 본문 18 Bold + 안읽음 4px 점 + 지역 pill·닉네임·상대시각 + **북마크 20**              | hue 그라데이션 블록 + 본문 + 우측 하트·시간 세로 스택 |
| 목록 빈 상태 | 84 원 + 36 letter 아이콘 + 16 Bold 한 줄                                                            | 회색 평문 한 줄                                       |
| 상세 상단    | 84 원 + 아이콘 + 14 Bold + 12 (중앙), 본문과 gap 40                                                 | 좌측 정렬 초록 배너                                   |
| 상세 카드    | `#F6F6F6` + 1px + r12, 60 사진 + 원형 도장 + From, **5칸 letterBox**(빨간 보더), 구분선 + To/도착일 | 베이지 편지지 + 점선 구획 + STAMP 배지                |
| 상세 액션    | [삭제 라인][저장 채움] + [편지함으로 초록 라인]                                                     | 좋아요/저장/삭제 3버튼                                |
| 발송완료     | 상세와 같은 카드 + 상단 블록                                                                        | 베이지 편지지                                         |

**시안에 좋아요가 없다.** 목록 행의 액션도 하트가 아니라 북마크이고 상세
버튼도 삭제/저장 둘이다. 그래서 UI 에서 좋아요를 제거했다 —
`useToggleLikeLetter` / `listLiked` API 는 코드에 남겨뒀으니 되살릴 때 UI 만
붙이면 된다.

상세와 발송완료가 같은 카드를 쓰므로 `LetterPaper` 로 한 컴포넌트로 뺐다.

남은 차이 — 시안의 페이지 배경은 `#F6F6F6` 이고 카드가 흰색인데, 레포는 흰
배경 + 보더 카드다. 편지뿐 아니라 마이페이지·설정 등 전 화면에 걸린 전역
결정이라 이번 범위에서는 손대지 않았다.

### 2.11 미판정이던 3개 — 전부 판정 완료 (2026-08-04)

초판의 "렌더 불충분" 은 데이터가 없어서가 아니라 **캡처 방법 문제**였다.

#### 캡처 방법 — 보호 화면은 API 를 가로채야 한다 (2026-08-04)

`/mypage/*` 는 3900(실 BE) 에서 401 이 나고, **axios 인터셉터가 401 을 받으면
`clearAuth()` 로 인증 시드를 지워** `/login` 으로 튕긴다. 그래서 localStorage 에
`tripbite.auth` 를 심고 `SID` cookie 를 넣어도 캡처가 안 된다.

해결 — CDP `Fetch.enable` 로 `*/api/backend/*` 를 가로채 **전부 200** 으로 응답한다
(`/me`, `/mypage/stamps`, `/mypage/tournaments` 는 목 JSON, 나머지는 `{}`).
`Fetch` 세션이 끊기면 다음 refetch 가 401 을 받아 화면이 에러로 바뀌므로,
클릭 인터랙션(삭제 모달 등)도 **같은 세션 안에서** 해야 한다.

또 하나 — 캡처 전처리로 PWA 배너를 `remove()` 하면 React 가 나중에 그 노드를
unmount 할 때 `removeChild ... not a child of this node` 로 죽어서 이후 상태 변경이
전부 실패한다 (삭제 모달이 안 열리던 원인). `style.display='none'` 으로 숨겨야 한다.

#### 2.11-2 `MY · 충북 도장책` → `/mypage/stamps` — 등급 C → 반영 (2026-08-04)

| 영역     | Figma (`3413:4083` / 마스터 `3413:4216`)                 | 구현 (수정 전)                    |
| -------- | -------------------------------------------------------- | --------------------------------- |
| 좌우여백 | 16 (본문 카드 328)                                       | 20 + 자체 16 = **36**             |
| 진행     | 흰 카드 + 큰 숫자 `N` / `11` + 라벨 + 8px 바 + 안내 문구 | sage 톤 배너 (숫자·바 없음)       |
| 지도     | 흰 카드(1px, r12, pad 20) 안                             | 카드 없이 본문에 바로             |
| 시군색   | 방문 `#EAF6EF`+`#00B334` / 미방문 흰+`#E0E0E0` **실선**  | sage-mist/sage / **dashed** muted |
| 라벨     | 10px Bold, 방문 초록 / 미방문 `#393939`                  | 24 SVG단위, 방문 sage / 미방문 fg |
| 범례     | 위 구분선 + 12px 스와치 2개                              | 없음                              |
| 마스터   | 카드 테두리 초록 + 라벨 초록 + 카드 안 공유 행           | 카드 밖 별도 버튼                 |

지도 라벨은 SVG viewBox 800 을 약 288px 로 그리므로(배율 0.36) 화면상 10px 이
되도록 SVG 단위 28px 로 뒀다. 실측 라벨 박스 높이 11px.

좌우 여백은 시안의 16 대신 **레포 전역 토큰 `--content-pad`(20) 를 유지**했다 —
한 화면만 16 으로 두면 다른 화면과 좌우 정렬이 어긋난다 (카드 폭 320 vs 시안 328).

새 문구 3개: `masterHint` `legendVisited` `legendUnvisited` (대응하는 기존 문구가
없어 시안 문구를 그대로 썼다). 나머지는 기존 문구 유지 — 시안의 "충북 마스터까지
3개" 대신 레포의 "…3개 남음" 을 쓴다.

#### 2.11-3 `MY · 저장한 우승지` → `/mypage/saved-tournaments` — 등급 B → 반영 (2026-08-04)

구조(안내 한 줄 + 2열 그리드 gap 12)는 이미 맞았고 규격만 어긋나 있었다.

| 영역     | Figma (`3402:2871` / 빈 `3402:3128` / 삭제 `3567:3835`) | 구현 (수정 전)                 |
| -------- | ------------------------------------------------------- | ------------------------------ |
| 좌우여백 | 20                                                      | 20 + 자체 16 = **36**          |
| 안내문   | `Caption/R_12` `#393939`                                | 14px                           |
| 하트     | 28×28 흰 원, 테두리 없음, 채운 하트 16                  | 32×32 반투명 + 빨강 테두리     |
| 하트위치 | 카드 상단/우측 10                                       | 6                              |
| 좌상단   | **없음**                                                | luckyColor 색점                |
| 빈상태   | 84 원 + **하트** 36 + 320×52 풀폭 버튼                  | 트로피 아이콘 + sm 버튼        |
| 모달     | 320 r18 pad 20/16 gap 20, 138×52 버튼 2개, 확인 초록    | 버튼 auto 폭, 확인 danger 빨강 |

실측: 카드 154×189(2줄 이름 고정 규칙 때문에 시안 168 보다 높다), 하트 28×28
@top11/right11(1px 테두리 포함), 모달 320×172 / 버튼 138×52.

부수 수정 — 시안 대조 중 공용 컴포넌트 2개의 내부 간격이 시안과 달랐다.
`Dialog` 는 제목+설명이 gap 4 인 한 블록이어야 하는데 20 으로 벌어져 있었고,
`EmptyState` 는 제목↔설명이 3 이어야 하는데 16~20 이었다. 둘 다 시안대로 고쳤다.

#### 2.11-1 `POI · 장소상세` → `/destination/[id]` — 등급 C → 반영 (2026-08-04)

실데이터(3900, `tour-2769769`)로 재캡처해 판정했다. 목 데이터 문제가 아니라
**대기 시간이 짧아 스켈레톤만 찍혔던 것**이 초판 "렌더 불충분" 의 실제 원인이다.

| 영역     | Figma (`3413:7268`)                              | 구현 (수정 전)                 |
| -------- | ------------------------------------------------ | ------------------------------ |
| hero     | 360×234 풀블리드 + 20% 검정 + 아래로 그라데이션  | 좌우 여백 안, 원형 썸네일      |
| dots     | 사진 위 하단 오버레이 (18×6 흰 / 6×6 `#8A8A8A`)  | 사진 아래 별도 줄              |
| 제목     | `Title/B_20_130%` + 영문 + 초록 pill 배지        | 제목 + 시군·카테고리 텍스트    |
| info     | **필드목록 → divider → overview** 순서, 껍데기 X | overview → 필드목록, 회색 카드 |
| 필드 행  | 18px 아이콘 + 80px 라벨 + `Caption/R_12`         | 16px + 92px + 14px SemiBold    |
| near-sec | 제목 `Basic Body/B_16_140%`                      | 18px ExtraBold                 |
| 액션     | 156×52 ×2, 채움 + 라인, radius 12                | 44px 높이, radius 12           |

반영 커밋 `a96c952`. 영문 보조 제목은 `DestinationDetailDto` 에 대응 필드가 없어
생략했다 (없는 데이터를 만들지 않는다).

`WinnerDetailPanel` 은 토너먼트 결과와 공유하는 컴포넌트인데, Figma 를 확인하니
두 화면 모두 **필드목록 → divider → overview** 순서였고 껍데기만 달랐다
(결과=흰 면+보더+radius 16, 상세=껍데기 없음). 그래서 순서는 공통으로 고치고
껍데기만 `variant="card" | "plain"` 으로 분리했다.

### 2.4 `설정` → `/settings` — 등급 B

섹션(알림 / 계정 / 계정 관리) + 토글 행 + 링크 행 구조가 일치한다.
Figma 3개 변형은 같은 화면의 상태 차이로 보인다. 세부 대조 필요.

---

## 3. 집계 (13개 검토, 2026-08-03)

| 등급 | 개수 | 화면                                                  |
| ---- | ---- | ----------------------------------------------------- |
| A    | 3    | 홈 · 편지 메인 · 마이페이지                           |
| B    | 5    | 설정 · 랭킹 · 시군상세 · 알림 목록 · (편지 하위 예상) |
| C    | 3    | 토너먼트 · 유형테스트 · 편지 쓰기                     |
| ?    | 0    | —                                                     |

미판정 3개는 2026-08-04 재캡처로 모두 판정했다 —
`장소상세` C→반영(§2.11-1) · `도장책` C→반영(§2.11-2) · `저장한 우승지` B→반영(§2.11-3).

**전면 재작업이 필요한 화면은 3개뿐이다.** 나머지는 스타일 수정이거나 거의 일치한다.
1~3단계에서 토큰·컴포넌트를 맞춰둔 효과로 보인다.

### ❌ 정정 — "헤더 부재" 는 오판이었다 (2026-08-03)

초판에 "Figma 는 하위 화면마다 헤더가 있는데 레포는 `/letter` 외에 없다" 고 적었으나
**사실이 아니다.** `SubHeader` 는 이미 거의 모든 라우트에서 쓰이고 있고, DOM 실측 결과
헤더가 정상 렌더된다.

| 라우트           | `h1` 실측                 | 위치     |
| ---------------- | ------------------------- | -------- |
| `/ranking`       | "여행지 랭킹"             | top=15px |
| `/notifications` | "알림"                    | top=15px |
| `/quiz`          | "여행 유형 테스트"        | top=15px |
| `/settings`      | "설정"                    | top=15px |
| `/tournament`    | "어떤 테마로 떠나볼까요?" | top=15px |
| `/mypage`        | **없음**                  | —        |

**오판 원인** — 캡처에 PWA 업데이트 배너(높이 60px, `top: 0`)가 sticky 헤더를 덮었다.
배너는 유지 결정된 개발 환경 요소이므로, 이후 캡처는 배너를 제거하고 한다.

→ 각 화면 상세표의 헤더 관련 항목도 모두 무효로 표시했다. 실제 헤더 관련 드리프트는
**`/mypage` 한 곳뿐**이다 (Figma 는 `뒤로가기 + "마이페이지" + 톱니`).

### 화면 전반에 공통으로 나타나는 드리프트

1. **묶음 카드 vs 개별 카드** — Figma 는 목록을 카드 하나 안에 넣고 행 구분선으로 나누는데
   레포는 항목마다 개별 카드를 쓴다. 랭킹 TOP5 · 랭킹 막대 · 설정 행 등에서 반복된다.
2. **섹션 헤더의 `전체 보기 >`** — Figma 는 섹션 제목 우측에 더보기 링크가 있다.
   홈 · 마이페이지에서 반복.
3. ~~**히어로의 사진 배경**~~ — **처리 완료.** 아래 §3-1 참고.

### 3-1. 히어로 — 처리 완료

Figma Plugin API 실측 결과 사진 배경 히어로는 **2곳**이다 (시군상세는 아니다).

| 위치     | Figma 노드          | 스펙                                                           |
| -------- | ------------------- | -------------------------------------------------------------- |
| 홈       | `hero-block > hero` | 320x176, radius 12, 텍스트 세로 중앙, eyebrow + 제목 + 핀/위치 |
| 랭킹 1위 | `visualCard > hero` | 288x176, radius 12, 텍스트 하단 좌측, 제목 + 시군/횟수         |
| 시군상세 | `banner`            | 320x103, **사진 아님** — 단색 `#EAF6EF` + 보더, 어두운 텍스트  |

공통 레이어 구조 (`components/ui/HeroCard`):

- `hero-img` — IMAGE(FILL) = `object-fit: cover`, 없으면 `#A8B29C` 플레이스홀더
  (`--color-media-placeholder`) + emoji
- `hero-overlay` — `gradientTransform` 항등행렬 = **좌→우** 축.
  `rgba(0,0,0,.72) 0% → .40 40% → 0 72% → 0 100%`
- `hero-text` — gap 4, 전부 `#FFFFFF`.
  `Caption/B_10` → `Title/B_20_130%` → `Caption/R_12`

Figma 에서 벗어난 3가지 (사유 포함):

1. **좌 padding 18 → 16** — 홈 인스턴스만 18px 로 그리드에서 벗어나 있었다.
   같은 컴포넌트의 랭킹 인스턴스가 쓰는 16px(`--space-4`) 로 통일. (4px 그리드 규칙)
2. **텍스트 블록 폭 78% → 62%** — 시안은 텍스트가 250/320 = 78% 까지 가는데
   scrim 은 72% 에서 이미 알파 0 이다. 시안 사진이 우측에서 중간톤이라 안 드러난
   문제로, 임의 사진에서는 흰 글씨가 흰 하늘 위에 놓인다.
3. **`text-shadow` 추가** (Figma 대응 없음) — 사진 위 흰 텍스트는 배경이 임의
   이미지라 대비 수치를 확정할 수 없다. 검정 윤곽을 얹어 순백 배경에서도 글자
   형태가 남게 한다. 스크림만으로는 우측 구간을 지킬 수 없다.

`aspect-ratio` 함정 하나 — 폭이 indefinite 한 컨테이너 안에서는 브라우저가
**min-content 높이 x 비율**로 폭을 역산한다. 실제로 홈에서 357 x 20/11 = 649px 로
부풀어 트랙을 넘겼다. 부모 grid item 에 `min-width: 0` 을 걸어 해결
(`layout.module.scss` 의 `minmax(0, 1fr)` 과 같은 취지).

## 3-2. 2026-08-03 반영 결과 (커밋 5개)

Figma Plugin API 로 화면별 노드를 실측해 아래를 반영했다. 각 항목은 커밋
메시지에 실측값(노드명 + 치수 + 텍스트 스타일)과 함께 기록돼 있다.

### 전역

| 항목            | 이전                           | 이후 (시안)                            |
| --------------- | ------------------------------ | -------------------------------------- |
| `--content-pad` | 16 (360 화면에서 12 까지 축소) | **20** — 시안 컨텐츠 폭 320            |
| 섹션 제목       | 18px extrabold                 | **16px Bold** (`Basic Body/B_16_140%`) |
| 섹션 헤더 간격  | 12                             | **16**                                 |
| "더보기" 색     | primary 초록                   | **보조 텍스트색**                      |

`PageSection` 에 `variant="card"` (시안 `rv-card`) 와 `titleScale="group"`
(시안 설정의 `bl` 라벨) 을 추가했고, 나머지 속성은 section 요소로 전달한다.

### 화면별

| 화면       | 반영                                                                 |
| ---------- | -------------------------------------------------------------------- |
| 홈         | 사진 hero, 섹션 제목 제거, 칩 필터 `rec-block`, 빠른시작 `qa-banner` |
| 랭킹       | 두 섹션을 `rv-card` 로 묶음, 1위 사진 hero, `top5-row`, `gun-row`    |
| 시군상세   | 단색 `banner`, pill 칩, `DestinationCard`                            |
| 설정       | 풀블리드 행, 하단 `bw` (outlined 로그아웃 + 텍스트 회원탈퇴)         |
| 알림       | 풀블리드 행 + 44px 원형 아이콘 + 안읽음 점 + 상대시각                |
| 마이페이지 | 헤더 톱니, `pf` 프로필 블록, `stamp-banner`, `recent-row` 묶음 카드  |
| 유형테스트 | 카운터/아이콘 제거, 세그먼트 8px 고정, 선택지 60px                   |
| 토너먼트   | 헤더 제목 "토너먼트" + 본문 단계 제목, `big-card`                    |
| 편지 쓰기  | 연결형 5칸, 회색 입력 카드, 좌측 카운터, 단일 버튼                   |
| 편지 메인  | 컴팩트 `banner`, 풀블리드 밑줄 탭, `letterItem` 82px                 |

### 신설한 문구 (2026-08-04 승인분)

그 전까지는 "문구는 기존 작업물대로" 원칙이라 시안 문구를 못 옮겼다. 아래만
새로 만들었고 나머지 화면은 여전히 기존 i18n 그대로다.

| 키                                        | 값 (ko)                                        |
| ----------------------------------------- | ---------------------------------------------- |
| `home.widgets.quick.tournamentBySeason.*` | "여름 토너먼트 시작하기" 등 (emoji 제거)       |
| `home.widgets.quick.tournamentHint`       | "16강부터 시작 · 약 2분 소요"                  |
| `home.widgets.quick.quiz`                 | "나는 어떤 여행자일까?"                        |
| `home.widgets.quick.quizHint`             | "5가지 질문으로 내 여행 유형 찾기"             |
| `home.widgets.quick.cta`                  | "시작"                                         |
| `home.picks.title` / `.hint`              | "이런 여행 어때요?" / "카테고리별 추천 여행지" |
| `home.picks.categories.*`                 | 전체 / 관광지 / 축제 / 체험관광                |
| `home.picks.empty`                        | "추천 여행지를 준비하고 있어요"                |

⚠ `tournamentHint` 의 "16강부터 시작" 은 시안 문구 그대로다. 레포 토너먼트는
셋업에서 8/16/32 를 고르므로 실제와 다를 수 있다 — 문구 확정 시 확인 필요.

### 시안대로 하지 않은 것 (사유 포함)

1. ~~**홈 카테고리 칩 필터**~~ — **반영 완료** (2026-08-04, 문구 신설 승인).
   `HomeCategoryPicks` 신규. `/rankings?type=recommended` + 축제 엔드포인트를
   합치고 카테고리 필터는 클라이언트에서 한다 (generated ranking client 가
   category 쿼리를 넘기지 않음). 구 `FestivalCarousel` 은 제거하고 D-day 뱃지를
   이 섹션이 흡수. 신규 문구는 `home.picks.*` (ko/en).
2. **토너먼트 하단 "다음" 버튼** — 시안은 카드 선택 후 확정하는 플로우지만
   레포는 카드 탭 시 즉시 진행한다. 플로우 변경이라 손대지 않았다.
3. **시군상세 인기도 chip** — 시안에 없으나 API 실데이터의 유일한 노출 지점.
4. **홈 캐러셀 점 5개** — 시안에 없다. 시안은 정지 상태의 1번 슬라이드.
5. **AA 미달 색** — 결정 2 대로 대체 (각 SCSS 주석에 측정값 기재).
   `#B4B4B4` 2.07:1 → `--color-muted`, `#00B334` 텍스트 2.80:1 →
   `--color-primary-text-bold`, 초록 위 흰 글씨 → `--color-primary-strong`,
   `#F79D26` 위 흰 글씨 → `--accent-amber`, `#E1493C` → `--color-danger`.

### 겸사겸사 고친 표시 버그

- 축제 카드 시군명이 `cheongju` 처럼 코드로 노출 → 한글명 변환
- 토너먼트 기록의 `undefined강` → 빈 조각 제외 후 " · " 결합
- 마이페이지에 AppHeader + SubHeader 두 개가 겹쳐 보임 → 화이트리스트 정리

### `aspect-ratio` 함정 (재발 방지)

폭이 indefinite 한 컨테이너 안에서 `aspect-ratio` 를 쓰면 브라우저가
**min-content 높이 x 비율** 로 폭을 역산한다. 홈 hero 가 357 x 20/11 = 649px
로 부풀어 그리드 트랙을 넘겼다. 부모 grid item 에 `min-width: 0` 을 건다
(`layout.module.scss` 의 `minmax(0, 1fr)` 과 같은 취지).

## 4. 다음 단계

1. **공통 드리프트 3개 처리** (§3) — 여러 화면이 한 번에 개선된다
2. **C 등급 3개 마감** — 헤더 추가 + 미세 차이 (편지 쓰기 입력 칸 형태 등)
3. **B 등급 5개** — 카드 묶음화 (히어로 사진화는 §3-1 로 완료)
4. **A 등급 3개** — 착수 전 개별 확인 필요 (아래)
5. **미판정 3개** — 목 데이터 보강 후 재캡처

### A 등급 착수 전 확인이 필요한 것

- **홈 칩 필터** — 새 기능. 단 `/region/[code]` 에 이미 같은 칩 필터가 구현되어 있어
  그 패턴을 재사용할 수 있다 (§2.6). 재사용 가능 여부 확인 필요
- ~~**홈 히어로** — 캐러셀 유지 vs Figma 의 단일 사진 카드~~ → **캐러셀 유지**로 진행.
  시안은 정지 상태의 1번 슬라이드를 그린 것으로 보고, 각 슬라이드를 사진 배경
  히어로로 교체했다. 점 5개 인디케이터는 남아 있다 (시안에는 없음).
- **편지 탭 "하트" ↔ Figma "저장한"** — 같은 기능인지
- **마이페이지 도장책 섹션** — Figma 는 진행률 바 + `8/11`. 진행률 데이터가 API 에 있는지
- **마이페이지 설정 진입** — Figma 는 헤더 톱니, 레포는 하단 버튼. 어느 쪽으로 통일할지
