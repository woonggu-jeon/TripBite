# E2E 시나리오 작성 / 실행 중 발견 이슈 추적 — 2026-06-01

> E2E 시나리오 (Phase 0 청소 + 신규 시나리오) 작성 / 실행 중 발견된 이슈를
> 빠뜨리지 않고 기록 + 처리.

---

## 진행 중 / 새 발견

(현재 추적 중인 신규 이슈 없음 — 4차 실행 결과 ECONNREFUSED 0건, 132 → 164 케이스 모두 통과)

---

## 처리 완료 (E2E 시나리오 실행 중 발견)

### B1. mock handler 부재 → proxy ECONNREFUSED ✅

- **상태**: 4차 실행 직후 발견 → 같은 turn 에 처리 완료
- **재현**: 4차 실행 (`flows.spec.ts` 추가 후) 의 webServer 로그
  ```
  [WebServer] Failed to proxy http://localhost:8080/mypage [ECONNREFUSED]
  [WebServer] Failed to proxy http://localhost:8080/settings [ECONNREFUSED]
  ```
- **원인**: 다음 endpoint 가 MSW handler 미정의 → MSW `onUnhandledRequest:'bypass'` 가 실 백엔드(localhost:8080) 로 보냄 → 백엔드 없음
  - `GET /mypage` — `mypageApi.getSummary`
  - `PATCH /mypage/profile` — `useUpdateNickname`
  - `GET /mypage/tournaments` — `tournamentApi.listSaved`
  - `DELETE /mypage/tournaments/:id` — `tournamentApi.removeSaved`
  - `GET /settings` — `settingsApi.get`
  - `PATCH /settings/notifications` — `settingsApi.updateNotifications`
- **처리**: `src/mocks/handlers.ts` 에 6 handler 신설 (mypage/settings 섹션). seed 재활용 (letterSeeds 의 saved/liked 필터, mockUser, myTravelType).
- **검증**: 5차 실행 — 164/164 통과 + ECONNREFUSED 0건.

---

## 처리 완료 (Phase 0 청소)

### A1. `features/quiz/` 폴더 dead ✅

- 폴더 통째 삭제 (4 컴포넌트 + hooks/api/types). 실 quiz 흐름은 `features/ranking/` 가 담당.

### A2. dead spec 7 파일 ✅

- `RegionAttractionList`, `RegionFestivalList`, `RegionExperienceList` — `RegionDetailTabs` 가 인라인 구현
- `ReceivedLetterList`, `SentLetterList`, `ManuscriptCard` — `LetterListPanel` / `LetterDetailClient` 실 구현
- `AccountActions` — 사이트맵 v2 후 settings link 만 사용

### A3. letter `'saved'` 일관성 버그 ✅

- 원인: `letterApi.listSaved` 누락 + `FETCHERS.saved` 미정의 + `LetterListKind` 에 'saved' 없음
- 처리: 3 곳 모두 보강 + i18n `letter.tabs.saved` / `letter.empty.saved` (ko/en)
- 결과: 편지함 4탭 (received/sent/liked/saved) 일관 동작 가능

### A4. `useTournamentHistory` hook 부재 ✅

- 원인: mock handler `GET /mypage/tournament-history` 와 seed 는 있으나 client 호출 함수 미존재
- 처리: `tournamentApi.listHistory` + `useTournamentHistory` + `tournamentKeys.history` 추가

### A5. 검증

- `tsc --noEmit` pass
- 회귀 import 없음 (dead 파일 어디서도 참조 안 함)

---

## 새 E2E 시나리오 — `flows.spec.ts`

| describe                    | 케이스                           | 검증                               |
| --------------------------- | -------------------------------- | ---------------------------------- |
| 온보딩 — 3-step 흐름        | 미인증 진입 → /onboarding        | URL 매칭                           |
| 동상                        | Concept step → 다음 진입 가능    | button 클릭                        |
| 편지 작성 — /letter/compose | 페이지 진입 + 입력 필드 노출     | textbox 가시성                     |
| 동상                        | 5글자 입력 후 submit 활성화      | Button enabled                     |
| 동상                        | 빈 / 6글자 초과 시 submit 분기   | disabled / invalid toast           |
| 토너먼트 random 흐름        | 홈 → 토너먼트 진입               | URL 매칭                           |
| 동상                        | step 4 "시작하기" 활성화         | Button enabled                     |
| 알림함                      | 알림 button → dialog + 항목 존재 | dialog + 자식 link/button 개수 ≥ 1 |

위치 권한: `context.grantPermissions(['geolocation'])` + `setGeolocation` 으로 사전 grant.

---

## 향후 발견 시 형식

```
### 카테고리. 항목명

- **상태**: 🟡 발견 / 🛠 진행 중 / ✅ 완료
- **위치**: `파일:라인`
- **재현**: ...
- **원인**: ...
- **처리**: ...
```
