# E2E 자동 QA 결과서 — 2026-06-01

> 1차 작성일: 2026-06-01 21:37 KST
> 최종 갱신: 2026-06-02 — 8차 (6 플랫폼 매트릭스 + share Desktop fallback + iOS appearance fix)

---

## 실행 환경

| 항목           | 값                                                   |
| -------------- | ---------------------------------------------------- |
| 빌드 모드      | `next build --turbopack && next start` (production)  |
| Mock           | `NEXT_PUBLIC_USE_MSW=true` (`src/mocks/handlers.ts`) |
| Service Worker | Serwist 활성 (production build)                      |
| Playwright     | 1.60.0                                               |
| webServer      | `localhost:3000` (Playwright 자동 기동)              |
| 명령           | `npm run test:e2e`                                   |

## 테스트 매트릭스 (Projects 6종 — 8차 이후)

| Project             | 디바이스                | Viewport | 용도               |
| ------------------- | ----------------------- | -------- | ------------------ |
| `desktop-windows`   | Desktop Chrome          | 1280×720 | Windows PC         |
| `desktop-mac`       | Desktop Chrome + Mac UA | 1440×900 | Mac PC             |
| `mobile-chrome-aos` | Pixel 7                 | 393×852  | AOS 모바일웹       |
| `mobile-safari-ios` | iPhone 14               | 390×844  | iOS 모바일웹       |
| `mobile-pwa-aos`    | Pixel 7 standalone      | 393×852  | AOS PWA (모바일앱) |
| `mobile-pwa-ios`    | iPhone 14 standalone    | 390×844  | iOS PWA (모바일앱) |

> 1~7차는 4 projects 매트릭스 (desktop-chrome / mobile-chrome / mobile-safari / mobile-pwa).

## 테스트 케이스 목록

### 1) `pages-smoke.spec.ts` — 페이지 진입 + 가로 overflow (Project 당 14 케이스)

| #   | 경로                                 | 검증                                                     |
| --- | ------------------------------------ | -------------------------------------------------------- |
| 1   | `/`                                  | 200 + body 비어있지 않음 + "TripBite/토너먼트/여행" 포함 |
| 2   | `/ranking`                           | 동상                                                     |
| 3   | `/tournament`                        | 동상                                                     |
| 4   | `/region`                            | 동상                                                     |
| 5   | `/region/cheongju`                   | 동상 (시군 상세)                                         |
| 6   | `/region/danyang`                    | 동상                                                     |
| 7   | `/letter`                            | 동상                                                     |
| 8   | `/letter/compose`                    | 동상                                                     |
| 9   | `/mypage`                            | 동상                                                     |
| 10  | `/settings`                          | 동상                                                     |
| 11  | `/quiz`                              | 동상                                                     |
| 12  | `/destination/cheongju-attraction-1` | 여행지 상세                                              |
| 13  | `/destination/danyang-festival-1`    | 여행지 상세                                              |
| 14  | 홈 헤더 알림 button + 하단 네비 노출 | a11y selector                                            |

가로 overflow 검증 — `document.documentElement.scrollWidth - clientWidth ≤ 1px`.

### 2) `og-routes.spec.ts` — `/api/og/*` Edge route (Project 당 5 케이스)

| #   | 케이스                          | 검증                                    |
| --- | ------------------------------- | --------------------------------------- |
| 1   | `/api/og/tournament?winner=...` | 200 + `image/png` + PNG 시그니처 + 1KB+ |
| 2   | `/api/og/quiz?type=...`         | 동상                                    |
| 3   | `/api/og/destination?id=...`    | 동상                                    |
| 4   | `/api/og/region?code=...`       | 동상                                    |
| 5   | `/api/og/unknown`               | 404                                     |

### 3) `interactions.spec.ts` — 도메인 라우팅 / 정책 (Project 당 7 케이스)

| #   | 케이스                                                        | 검증                     |
| --- | ------------------------------------------------------------- | ------------------------ |
| 1   | 홈 - 축제 카드 클릭                                           | `/destination/{id}` 진입 |
| 2   | 랭킹 - Top5 카드 클릭                                         | `/destination/{id}` 진입 |
| 3   | 랭킹 - 시군별 행 클릭                                         | `/region/{code}` 진입    |
| 4   | 시군 상세의 콘텐츠 row 클릭                                   | `/destination/{id}` 진입 |
| 5   | CategoryFilter 에 '지역(local)' 카드 없음                     | radio count = 0          |
| 6   | 헤더 알림 button → 드롭다운 열림                              | role=dialog visible      |
| 7   | 홈 빠른시작에 '편지 쓰기' 없음, 계절 토너먼트 + 유형 테스트만 | text 매칭                |

### 4) `smoke.spec.ts` — 기존 (Project 당 3 케이스)

| #   | 케이스                                    | 검증                |
| --- | ----------------------------------------- | ------------------- |
| 1   | 미인증 → /login \| /onboarding 리다이렉트 | URL 패턴 매칭       |
| 2   | /login 로드                               | 200                 |
| 3   | /api/health 200                           | json `{ ok: true }` |

---

## 1차 실행 결과

| 항목        | 값                                           |
| ----------- | -------------------------------------------- |
| 총 케이스   | **116** (4 projects × 29 cases)              |
| **통과**    | **116 / 116 (100%)**                         |
| 실패        | 0                                            |
| 실행 시간   | 27.6s                                        |
| 환경 안정성 | ✅ webServer 자동 기동 + reuseExistingServer |

### 시도 1 → 시도 2 사이 fix 한 항목

| 이슈                                                        | 원인                                                                       | 처리                                            |
| ----------------------------------------------------------- | -------------------------------------------------------------------------- | ----------------------------------------------- |
| webkit binary 미설치 → mobile-safari / mobile-pwa 50개 실패 | Playwright 1.60.0 새 버전에 webkit-2287 바이너리 자동 미포함               | `npx playwright install webkit`                 |
| `홈 헤더 / 하단 네비 노출` strict mode violation            | aria "Home" 이 로고 + 하단 네비 2 곳에 매칭                                | `.first()` 추가                                 |
| `미인증 → /login` 실패                                      | middleware redirect 비활성 상태라 AuthBootstrap 이 `/onboarding` 으로 보냄 | URL 패턴 `/(login\|onboarding)` 로 완화         |
| 알림 button click intercept                                 | `Banner` (Install/Update/MockMode) 가 위에 떠 click 막음                   | `dismissBanners` 헬퍼 — pointer-events 차단 CSS |

---

## 2차 실행 결과 — 안정성 검증

| 항목      | 값                              |
| --------- | ------------------------------- |
| 총 케이스 | **116** (4 projects × 29 cases) |
| **통과**  | **116 / 116 (100%)**            |
| 실패      | 0                               |
| 실행 시간 | 28.6s                           |

**판정**: 두 번 연속 동일 결과 (116/116) — 회귀 / 환경 의존성 / race condition **없음** 으로 판정.

---

## 3차 실행 — 결과서 발견 이슈 fix 후 재검증

### fix 한 항목

| 항목                                      | 처리                                                                                                                                                                      | 영향                                                                                        |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| **Banner click intercept** (운영 전 필수) | `src/features/pwa/components/Banner.module.scss` 에 `pointer-events: none` + `.banner > *` 자식만 `auto`. message text 는 pass-through, action/close button 만 click 받음 | `dismissBanners` 헬퍼 제거 가능. 운영 환경에서도 banner 가 header 의 button click 막지 않음 |
| **테스트 커버리지 확장**                  | 새 인터랙션 시나리오 4개 추가 (interactions.spec.ts)                                                                                                                      | 토너먼트 랜덤/계절 흐름 + query prefill + share button 검증                                 |

### 추가된 케이스 (interactions.spec.ts)

| #   | 케이스                                            | 검증                          |
| --- | ------------------------------------------------- | ----------------------------- |
| 8   | 랜덤 테마 선택 → step 4 점프                      | "시작하기" 버튼 노출          |
| 9   | 계절 직접선택 → step 2 노출                       | SeasonSelector radio 노출     |
| 10  | `?theme=season&season=spring` query → step 3 부터 | CategoryFilter 3 옵션 노출    |
| 11  | 여행지 상세 share IconButton 노출                 | SubHeader 우측 button visible |

### 3차 결과

| 항목      | 값                              |
| --------- | ------------------------------- |
| 총 케이스 | **132** (4 projects × 33 cases) |
| **통과**  | **132 / 132 (100%)**            |
| 실패      | 0                               |
| 실행 시간 | 1.2m (clean rebuild 포함)       |

**판정**: Banner CSS fix 정상 적용 (production build clean) + 추가 시나리오 4개 모두 통과. **dismissBanners 헬퍼 우회 없이도 동작** — 운영 환경 안전.

---

## 4차 — Phase 0 청소 + 새 시나리오 (`flows.spec.ts`)

### Phase 0 청소

- `features/quiz/` 폴더 통째 삭제
- dead spec 7 파일 삭제 (Region/Letter/Mypage)
- `letterApi.listSaved` + `FETCHERS.saved` + `LetterListKind` 'saved' 보강
- `useTournamentHistory` + `tournamentApi.listHistory` 신설
- i18n `letter.tabs.saved` / `letter.empty.saved` (ko/en)

### 신규 spec `flows.spec.ts` — 8 케이스

| describe        | 케이스                                       |
| --------------- | -------------------------------------------- |
| 온보딩          | 미인증 진입 → /onboarding, Concept step 진입 |
| 편지 작성       | 페이지 진입, 5글자 submit 활성, 빈/초과 분기 |
| 토너먼트 random | 홈 → 토너먼트 진입, step 4 시작 활성         |
| 알림함          | 알림 button → dialog + 항목                  |

### 4차 결과

| 항목           | 값                                                                                               |
| -------------- | ------------------------------------------------------------------------------------------------ |
| 총 케이스      | **164** (4 projects × 41 cases)                                                                  |
| **통과**       | **164 / 164 (100%)**                                                                             |
| 실패           | 0                                                                                                |
| 실행 시간      | 1.2m                                                                                             |
| **proxy 회귀** | ⚠️ `GET /mypage`, `GET /settings` ECONNREFUSED 로그 발견 (테스트는 통과지만 실 백엔드 호출 시도) |

---

## 5차 — proxy 회귀 fix 후 재검증

### fix (이슈 추적 B1)

mock handler 6개 추가:

- `GET /mypage` — 프로필/저장/좋아요 편지/여행 유형 요약
- `PATCH /mypage/profile` — 닉네임 변경
- `GET /mypage/tournaments` — 저장된 우승지 목록
- `DELETE /mypage/tournaments/:id` — 저장 삭제
- `GET /settings` — 알림 설정
- `PATCH /settings/notifications` — 알림 toggle

### 5차 결과

| 항목           | 값                              |
| -------------- | ------------------------------- |
| 총 케이스      | **164** (4 projects × 41 cases) |
| **통과**       | **164 / 164 (100%)**            |
| 실패           | 0                               |
| 실행 시간      | 1.2m                            |
| **proxy 회귀** | ✅ **0건 — 완전 해소**          |

**최종 판정**: 5차 연속 안정 + proxy 깔끔 + 발견 이슈 모두 처리 완료.

---

## 6차 — Phase 1 mypage 위젯 통합 후 회귀 검증

### 통합한 위젯 (Placeholder → 실제 컴포넌트)

| 섹션               | 컴포넌트                   | 비고                                              |
| ------------------ | -------------------------- | ------------------------------------------------- |
| 닉네임             | `NicknameSection`          | `useMypage` + `useUpdateNickname` + edit Dialog   |
| 저장한 우승 여행지 | `SavedTournamentsSection`  | `useSavedTournaments` + remove confirm            |
| 토너먼트 기록      | `TournamentHistorySection` | `useTournamentHistory` (단일 page, 최대 10)       |
| 편지함 4탭         | `LetterboxTabs`            | received/liked/saved/sent — lazy mount + prefetch |

도장깨기 (`stampMap`) 는 Phase 2 로 보류 — ChungbukSvgMap + `/mypage/stamps` API 의존.

### i18n 추가 (ko/en)

- `mypage.nickname.*` (label/edit/editTitle/loading/invalid/saved/saveFailed)
- `mypage.savedTournaments.*` (empty/emptyHint/startTournament/error/retry/remove/removed/removeFailed/removeConfirm\*)
- `mypage.tournamentHistory.*` (empty/error/retry/countUnit)
- `mypage.sections.nickname`

### 6차 결과

| 항목      | 값                              |
| --------- | ------------------------------- |
| 총 케이스 | **164** (4 projects × 41 cases) |
| **통과**  | **164 / 164 (100%)**            |
| 실패      | 0                               |
| 실행 시간 | 6.1m                            |
| 회귀      | 0                               |

**판정**: 6차 연속 안정 — Phase 1 mypage 위젯 4종 통합 + i18n 갱신 후 회귀 0건.

---

## 7차 — Phase 2 (도장깨기 + middleware 복원 + axe + 시각 회귀)

### 통합 / 변경

| 항목                             | 내용                                                                                                                                                                              |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 도장깨기 (RegionStampMap)        | `ChungbukSvgMap` (5×3 grid SVG, 11 시군) + `useStamps` + 진행률. 정밀 path 데이터는 후속 PR.                                                                                      |
| `/mypage/stamps` mock            | deterministic `visited: [cheongju, chungju, jecheon, boeun, danyang]`                                                                                                             |
| middleware 인증 redirect 복원    | `PUBLIC_ACCESS_PATHS` 신설 (auth flow + /onboarding + /policy + /offline). 그 외 cookie 없으면 `/login` redirect. 위치도 `src/middleware.ts` 로 이동 (Next.js 15 src/ 구조 인식). |
| E2E 헬퍼 통합 `_helpers/auth.ts` | `injectAuthCookie` + `bypassOnboarding` 결합한 `authedSession` — middleware 복원과 함께 cookie 주입.                                                                              |
| smoke.spec / flows.spec          | 비인증 → `/login` 으로 패턴 좁힘 + `/onboarding` 직접 접근 가능 케이스 추가                                                                                                       |
| `e2e/a11y.spec.ts` 신설          | `@axe-core/playwright` — 6 페이지 (/ /mypage /letter /tournament /region /ranking) serious+ 위반 0건 검증                                                                         |
| `e2e/visual.spec.ts` 신설        | `toHaveScreenshot` baseline — 4 페이지 × 2 모드 (light/dark) = 8 baseline. 현재 desktop-chrome only.                                                                              |

### 추가된 케이스 / Project 매트릭스 변화

- a11y: +6 cases (desktop-chrome 만)
- visual: +8 cases (desktop-chrome 만)
- flows: +1 case (`/onboarding` 비인증 접근)

### 7차 결과

| 항목             | 값                                                                                                                          |
| ---------------- | --------------------------------------------------------------------------------------------------------------------------- |
| 총 케이스 (전체) | **244 passed** (4 projects, 36 skipped — desktop-only visual/mobile-360)                                                    |
| desktop-chrome   | 70 (smoke + interactions + flows + a11y 6 + visual 8 + mobile-360 4 + tournament-full 2 + location 6 + push 2 + og + pages) |
| mobile-chrome    | 58 (visual/mobile-360 skipped)                                                                                              |
| mobile-safari    | 58                                                                                                                          |
| mobile-pwa       | 58                                                                                                                          |
| 실행 시간        | 6.2m (4 projects)                                                                                                           |
| 회귀             | 0                                                                                                                           |
| a11y serious+    | 0 (color-contrast 제외 — 디자인 baseline)                                                                                   |
| 시각 baseline    | 8 (home/mypage/letter/region-cheongju × light/dark)                                                                         |

**판정**: 7차 — Phase 2 4종 + Phase 5 일부 (a11y / 시각 / middleware) + Phase 6 일부 (테마 토글 / 14세 step) + 추가 E2E (mobile-360 / 위치 권한 5종 / 토너먼트 풀 / 푸시) 모두 통합. 회귀 0. middleware 복원으로 unauth → /login 동작 정상화. 시각 baseline + a11y 자동화로 회귀 검출 폭 확장.

### 알려진 잡음 — 운영 영향 없음

- CSP report-only 로그: Pretendard CDN font preload 가 `connect-src https://cdn.jsdelivr.net` 미허용으로 report. report-only 라 차단 X, font-src 는 정상 허용. 별 PR 로 CSP 정리 필요.

---

## 8차 — 플랫폼 매트릭스 6종 + 시각 baseline 전체 캡처

### Projects 매트릭스 (4 → 6)

| Project             | 디바이스                | Viewport | 용도               |
| ------------------- | ----------------------- | -------- | ------------------ |
| `desktop-windows`   | Desktop Chrome          | 1280×720 | Windows PC         |
| `desktop-mac`       | Desktop Chrome + Mac UA | 1440×900 | Mac PC             |
| `mobile-chrome-aos` | Pixel 7                 | 393×852  | AOS 모바일웹       |
| `mobile-safari-ios` | iPhone 14               | 390×844  | iOS 모바일웹       |
| `mobile-pwa-aos`    | Pixel 7 standalone      | 393×852  | AOS PWA (모바일앱) |
| `mobile-pwa-ios`    | iPhone 14 standalone    | 390×844  | iOS PWA (모바일앱) |

### middleware mock skip

운영(`USE_MSW=false`): cookie 없으면 `/login` redirect 유지.
mock(`USE_MSW=true`): 백엔드 cookie 발급 불가 → middleware redirect skip → 모든 페이지 접근 가능.

### 시각 baseline 확장 (8 → 48)

- 4 페이지 (home / mypage / letter / region-cheongju) × 2 모드 (light/dark) × 6 projects = **48 baseline**
- viewport 캡처 (above-the-fold). fullPage 는 무한 스크롤 페이지의 dynamic height 변동으로 baseline 불안정 → viewport 로 회귀.
- threshold: `maxDiffPixelRatio: 0.05`, `animations: 'disabled'`, `document.fonts.ready` 대기.

### 8차 결과

| 항목          | 값                                                                          |
| ------------- | --------------------------------------------------------------------------- |
| 총 케이스     | **420** (6 projects × 70 cases)                                             |
| 통과          | **404 passed**                                                              |
| 실패          | 0                                                                           |
| Skipped       | 16 (mobile-360 × 4 mobile projects — viewport override vs device emulation) |
| 실행 시간     | 6.7m                                                                        |
| 시각 baseline | 48                                                                          |
| 회귀          | 0                                                                           |

**판정**: 8차 — 6 플랫폼 매트릭스 (Windows / Mac / AOS web / iOS web / AOS PWA / iOS PWA) 전체 통과. 시각 baseline 48장으로 플랫폼 별 레이아웃/CSS 깨짐 회귀 자동 검출 wired.

### 스크린샷 산출물

- `e2e/visual.spec.ts-snapshots/*.png` — 48 baseline (git 추적, PR diff 가능)
- 회귀 발생 시: `test-results/<spec>/<case>-<project>/{actual,expected,diff}.png` 자동 생성

---

## 발견 이슈 / 향후 작업

### A. 운영 전 필수 점검

| 항목                        | 현재                                | 운영 전 처리                                                                                     |
| --------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------ |
| middleware 의 인증 redirect | 비활성 (`middleware.ts:46-58` 주석) | 주석 해제 + smoke.spec 의 redirect 검증 `/login` 으로 다시 좁힘                                  |
| ~~Banner click intercept~~  | ~~`dismissBanners` 헬퍼 우회~~      | ✅ **3차에서 처리 완료** — Banner.module.scss `pointer-events: none` + `.banner > *` 자식만 auto |

### B. 테스트 커버리지 확장 (Phase 5)

다음 시나리오는 아직 미커버 — Playwright 로 점진 확장 권장.

- [ ] 온보딩 3-step 시나리오 (concept → location → finish)
- [ ] 편지 작성 → /letter/sent 진입
- [ ] 위치 권한 5종 시나리오 (granted/prompt/denied/IP fallback/실패)
- [ ] 토너먼트 setup → play → result 전체 플로우 (random 흐름 포함)
- [ ] Push prompt → mock trigger → 알림 클릭 → letter detail
- [ ] 다크 모드 자동 전환 (`prefers-color-scheme`)

### C. CI 통합

- `.github/workflows/e2e.yml` 신설 — PR 시 desktop-chrome 만 (속도 우선) / nightly main 에 4 projects
- chromium / webkit binary 캐시 (build 시간 단축)

### D. 시각 회귀 (visual regression)

- 현재 가로 overflow 만 검증 — 디자인 깨짐은 못 잡음
- 향후 `@playwright/test` 의 `toHaveScreenshot()` 도입 — 페이지별 baseline 스크린샷 diff
- 새 위젯 구현 시점에 추가 (디자인 시안 확정 후)

---

## 권장 작업 순서

### 즉시 (이번 turn)

1. ✅ 2차 실행 결과 확인 — 안정성 검증
2. (실패 시) fix → 3차 실행

### 단기 (1-2일)

3. CI `.github/workflows/e2e.yml` 추가 (desktop-chrome 만 PR, 모바일 매트릭스 nightly)
4. 온보딩 / 편지 작성 / 위치 권한 E2E 시나리오 추가

### 중기 (Phase 1-2 진행 시)

5. mypage / 홈 위젯 구현 후 해당 페이지의 인터랙션 E2E 케이스 추가
6. 가로 overflow 외에 **세로 overflow / 키보드 trap / focus 순서** 추가 검증

### 장기 (운영 진입 전)

7. middleware 인증 redirect 복원 + smoke spec 의 redirect 검증 `/login` 으로 좁힘
8. visual regression (`toHaveScreenshot`) — 디자인 시안 확정 후
9. accessibility — `@axe-core/playwright` 통합

---

## 부록: 핵심 헬퍼

```ts
// e2e/interactions.spec.ts + pages-smoke.spec.ts 공통

async function bypassOnboarding(page: Page) {
  await page.addInitScript(() => {
    localStorage.setItem('tripbite.onboarded', 'true');
    localStorage.setItem('tripbite.push-prompt.dismissed', 'true');
  });
}

async function dismissBanners(page: Page) {
  await page.addStyleTag({
    content: `
      [class*="MockModeBanner"],
      [class*="InstallPromptBanner"],
      [class*="PwaUpdateBanner"],
      [class*="OfflineBanner"],
      [class*="Banner_banner"] {
        pointer-events: none !important;
        opacity: 0.1 !important;
      }
    `,
  });
}

async function assertNoHorizontalOverflow(page: Page) {
  const overflow = await page.evaluate(() => {
    const w = document.documentElement.clientWidth;
    const scroll = document.documentElement.scrollWidth;
    return scroll - w;
  });
  expect(overflow).toBeLessThanOrEqual(1);
}
```

---

## 이슈 추적 (모두 처리 완료)

### Phase 0 청소 (1~4차)

| ID  | 항목                             | 처리                                                                                                                      |
| --- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| A1  | `features/quiz/` 폴더 dead       | 통째 삭제 (실 quiz 흐름은 `features/ranking/`)                                                                            |
| A2  | dead spec 7 파일                 | RegionAttractionList/FestivalList/ExperienceList, ReceivedLetterList/SentLetterList/ManuscriptCard, AccountActions — 삭제 |
| A3  | letter `'saved'` 일관성 버그     | `letterApi.listSaved` + `FETCHERS.saved` + `LetterListKind` 'saved' 보강 + i18n                                           |
| A4  | `useTournamentHistory` hook 부재 | `tournamentApi.listHistory` + `useTournamentHistory` + `tournamentKeys.history` 추가                                      |

### 실행 중 발견 (5~8차)

| ID  | 항목                                                  | 처리                                                              |
| --- | ----------------------------------------------------- | ----------------------------------------------------------------- |
| B1  | mock handler 부재 → proxy ECONNREFUSED (`/mypage` 등) | handler 7 신설 (5차)                                              |
| B2  | Banner click intercept                                | `Banner.module.scss` `pointer-events: none` + 자식 `auto` (3차)   |
| B3  | middleware redirect 운영-mock 충돌                    | mock 환경 (`USE_MSW=true`) 한정 redirect skip (8차)               |
| B4  | 카카오톡 채팅 첨부 흐름 끊김                          | `shareWithImage` payload 에서 title/text 제거 → file 단독         |
| B5  | iOS Safari/PWA 토너먼트 선택 테두리 사라짐            | `_reset.scss` button 에 `appearance: none` 추가                   |
| B6  | Desktop file share 미지원 환경에서 다운로드만 발생    | `shareWithImage` 미지원 분기 — URL clipboard copy + 다운로드 동시 |
| B7  | visual baseline fullPage dynamic height drift         | viewport 캡처로 회귀 (`maxDiffPixelRatio: 0.05`)                  |

향후 발견 시 형식:

```
### 카테고리. 항목명
- 상태: 🟡 발견 / 🛠 진행 중 / ✅ 완료
- 위치: `파일:라인`
- 재현: ...
- 원인: ...
- 처리: ...
```
